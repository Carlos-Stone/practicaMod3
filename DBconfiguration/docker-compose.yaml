services:
  postgres:
    build: . 
    container_name: EjemploBD 
    ports:
      - "5432:5432" 
    environment:
      POSTGRES_USER: Admin 
      POSTGRES_PASSWORD: pap4ssw0rdDB
      POSTGRES_DB: credenciales 
    volumes:
      - postgres_data:/var/lib/postgresql 


volumes:
  postgres_data:
    driver: local
